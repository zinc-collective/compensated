RSpec.describe Compensated::Ruby::Stripe do
  it "has a version number" do
    expect(Compensated::Ruby::Stripe::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
